## 23.合并k个升序链表
- **标签：** 链表/归并排序
- **原题链接：** [点击跳转](https://leetcode.cn/problems/merge-k-sorted-lists/description/?envType=study-plan-v2&envId=top-100-liked)

### 💡 思路历程
对归并中分治的写法还是很不熟悉，t148因为其是一整条链表，自上而下时所以采用找中点的方法。而本题这些链表放在数组里，其实完全可以用数组的逻辑，
### 💻 C++ 递归极简代码实现
```cpp
 class Solution {
public:
    bool check(TreeNode *p, TreeNode *q) {
        if (!p && !q) return true;
        if (!p || !q) return false;
        return p->val == q->val && check(p->left, q->right) && check(p->right, q->left);
    }

    bool isSymmetric(TreeNode* root) {
        return check(root->left, root->right);
    }
};
```
### 🔥值得注意
迭代法，肯定是用队列，after all是一行一行比的，但我一开始想的是不将不合法的点和空结点放进入队列里，所以在放入前就要判断很多东西，**事实上完全可以将nullptr放进队列**，出来才统一判断，代码简洁很多。见下即可。

### 💻 C++ 迭代复杂代码实现
```cpp
class Solution {
public:
    
    bool isSymmetric(TreeNode* root) {
       queue<TreeNode*> que;
       if(!root->left&&!root->right) return true;
       if(!root->left||!root->right) return false;
       que.push(root->left);
       que.push(root->right);
       while(!que.empty()){
            TreeNode* node1 = que.front();que.pop();
            TreeNode* node2 = que.front();que.pop();
            if(node1->val!=node2->val) return false;
            if(node1->left&&node2->right) {
                que.push(node1->left);
                que.push(node2->right);
            }
            if(node1->left&&!node2->right) return false;
            if(!node1->left&&node2->right) return false;
            if(node1->right&&node2->left) {
                que.push(node1->right);
                que.push(node2->left);
            }
            if(node1->right&&!node2->left) return false;
            if(!node1->right&&node2->left) return false;
       }
       return true;
    }
};
```

### 💻 C++ 迭代简洁代码实现
```cpp
class Solution {
public:
    bool isSymmetric(TreeNode* root) {
       queue<TreeNode*> que;
       if(root == nullptr) return true;
       else { 
        que.push(root->left);
        que.push(root->right);
       }
        while(!que.empty()){
            TreeNode* leftnode = que.front();que.pop();
            TreeNode* rightnode = que.front(); que.pop();
            if(!leftnode&&!rightnode) continue;
            if(!leftnode||!rightnode||leftnode->val!=rightnode->val) return false;

            que.push(leftnode->left);
            que.push(rightnode->right);
            que.push(leftnode->right);
            que.push(rightnode->left);
        }
        return true;
    }
};
```
